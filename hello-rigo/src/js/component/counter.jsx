import React from 'react'

const Counter = () => {
    return (
        
        let seconds = 0;

        function init(){
            setInterval(newseconds,1000);
        }

        function newseconds(){
            seconds = seconds+1;
        }
    
    )
}

export default Counter
